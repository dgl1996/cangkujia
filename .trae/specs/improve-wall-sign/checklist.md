# Checklist

## Phase 1: 核心功能优化

### Z-fighting 修复检查项

- [x] `createWallSign()` 材质添加 `polygonOffset: true`
- [x] `createWallSign()` 材质添加 `polygonOffsetFactor: -1`
- [x] 标语在墙面上显示无闪烁
- [x] 不同角度观察标语均清晰

### 父子级关联检查项

- [x] 标语 Mesh 添加到墙体对象的 `children` 中
- [x] 标语不再直接添加到 `sceneObjects` 数组
- [x] 墙体移动时标语跟随移动
- [x] 标语旋转与墙体保持一致
- [x] 标语位置相对墙体正确

### 标语更新逻辑检查项

- [x] `updateWallSign()` 保持父子级关联不变
- [x] 更新后标语仍贴合墙面
- [x] 更新后标语位置不偏移
- [x] 更新后无 Z-fighting 问题

## Phase 2: 保存/加载逻辑

### 保存逻辑检查项

- [x] `getSceneObjects()` 能获取到标语（通过墙体 children）
- [x] 保存的标语数据包含 `wallIndex`
- [x] 保存的标语数据包含 `offsetAlongWall`
- [x] 保存的标语数据包含 `text/fontSize/textColor/bgColor`
- [x] JSON 数据结构正确

### 加载逻辑检查项

- [x] 导入时能根据 `wallIndex` 找到对应墙体
- [x] 导入时调用 `createWallSign()` 重新创建标语
- [x] 导入后标语位置正确
- [x] 导入后标语样式正确
- [x] 导入后标语与墙体建立父子级关联

## Phase 3: 功能测试

### 基础功能检查项

- [x] 可以拖拽放置标语到前墙
- [x] 可以拖拽放置标语到后墙
- [x] 可以拖拽放置标语到左墙
- [x] 可以拖拽放置标语到右墙
- [x] 标语默认显示文字正确
- [x] 点击"编辑标语"弹出编辑对话框
- [x] 可以修改文字内容（支持中英文）
- [x] 可以修改字体大小
- [x] 可以修改文字颜色
- [x] 可以修改背景颜色
- [x] 编辑时实时预览效果
- [x] 点击移动按钮可以沿墙面移动
- [x] 移动时标语不离开墙面
- [x] 点击删除按钮可以删除标语

### 保存/加载检查项

- [x] 保存项目包含标语数据
- [x] 导入项目恢复标语显示
- [x] 导入后标语位置准确
- [x] 导入后标语样式正确

## Phase 4: 回归测试

### 兼容性检查项

- [x] 门/窗功能不受影响
- [x] 立柱功能不受影响
- [x] 其他模型功能不受影响
- [x] 对齐线功能不受影响
- [x] 保存/加载其他对象正常

### 性能检查项

- [x] 标语数量多时（10+）性能正常
- [x] 切换视角时无卡顿
- [x] 编辑标语时响应流畅
